import { readFileSync, writeFileSync } from "node:fs";

const htmlPath = "dist/index.html";
let html = readFileSync(htmlPath, "utf8");
const marker = "NumeriaDivinationSettingsMenuFallback.v1";
if (html.includes(marker)) {
  throw new Error("Divination settings menu fallback was applied more than once.");
}

const script = `<script id="numeria-divination-settings-menu-fallback">(()=>{const MARK="${marker}",OVERLAY_ID="numeria-divination-settings-fallback";const OPTIONS=[{id:"numerology",label:"数秘術",desc:"生年月日や名前から基本傾向を読む"},{id:"tarot",label:"タロット",desc:"カードから現在の流れを読む"},{id:"four-pillars",label:"四柱推命",desc:"生年月日時から命式を読む"},{id:"western-astrology",label:"西洋占星術",desc:"星の配置から傾向を読む"}];function textOf(node){return String(node&&node.textContent||"").replace(/\\s+/g,"").trim()}function isTarget(node){let text=textOf(node);return text.includes("占術設定")||text.includes("占術の変更")||text.includes("占術変更")||text.includes("DIVINATIONSETTINGS")}async function authHeaders(){let headers=new Headers({"Content-Type":"application/json","X-Workspace-Id":"ws_personal"});try{if(window.Clerk&&window.Clerk.session&&typeof window.Clerk.session.getToken==="function"){let token=await window.Clerk.session.getToken();if(token)headers.set("Authorization","Bearer "+token)}}catch{}return headers}async function fetchJson(url,options={}){let response=await fetch(url,{cache:"no-store",...options});let body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.message||"通信に失敗しました");return body}async function state(){let headers=await authHeaders();let [admin,billing,prefs]=await Promise.all([fetchJson("/api/admin/status",{headers}).catch(error=>({error:String(error.message||error)})),fetchJson("/api/billing/subscription?workspaceId=ws_personal",{headers}).catch(()=>({subscription:{planId:"free"}})),fetchJson("/api/user-preferences?workspaceId=ws_personal",{headers}).catch(()=>({preferences:{primary_divination:"numerology",enabled_divinations:["numerology"]}}))]);let preview=admin&&admin.developerPreview||null;let adminMode=!!(admin&&admin.adminMode&&preview&&preview.businessUiPreviewEnabled);return{headers,adminMode,actualPlan:billing&&billing.subscription&&billing.subscription.planId||"free",preferences:prefs&&prefs.preferences||{primary_divination:"numerology",enabled_divinations:["numerology"]}}}function styles(){if(document.getElementById("numeria-divination-settings-fallback-style"))return;let style=document.createElement("style");style.id="numeria-divination-settings-fallback-style";style.textContent="#"+OVERLAY_ID+"{position:fixed;inset:0;z-index:2147483646;background:rgba(22,18,37,.32);display:grid;align-items:end;font-family:system-ui,-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif}#"+OVERLAY_ID+" .nd-panel{background:#fffdf8;border-radius:26px 26px 0 0;border:1px solid #e6dfd1;box-shadow:0 -22px 60px rgba(22,18,37,.22);padding:22px 18px calc(28px + env(safe-area-inset-bottom));max-height:82vh;overflow:auto;color:#171326}#"+OVERLAY_ID+" .nd-head{display:flex;justify-content:space-between;gap:12px;align-items:flex-start;margin-bottom:12px}#"+OVERLAY_ID+" h2{margin:0;font-family:Georgia,'Yu Mincho',serif;font-size:25px;font-weight:500}#"+OVERLAY_ID+" p{margin:5px 0 0;color:#7c7582;font-size:13px;line-height:1.7}#"+OVERLAY_ID+" .nd-close{border:1px solid #ded7ca;background:#fff;border-radius:999px;width:40px;height:40px;font-size:22px;color:#5d5668}#"+OVERLAY_ID+" .nd-status{background:#f7f1e5;border-left:4px solid #c4a65d;border-radius:12px;padding:10px 12px;font-size:13px;line-height:1.7;margin:12px 0;color:#5f5132}#"+OVERLAY_ID+" .nd-options{display:grid;gap:10px;margin:14px 0}#"+OVERLAY_ID+" .nd-option{display:flex;gap:10px;align-items:flex-start;border:1px solid #e6dfd1;border-radius:16px;background:#fff;padding:13px 12px;text-align:left}#"+OVERLAY_ID+" .nd-option input{margin-top:4px;transform:scale(1.2)}#"+OVERLAY_ID+" .nd-option strong{display:block;font-size:16px;color:#171326}#"+OVERLAY_ID+" .nd-option span{display:block;font-size:12px;color:#847b87;margin-top:3px}#"+OVERLAY_ID+" .nd-primary{margin:12px 0;display:grid;gap:6px;font-size:13px;color:#6d6471}#"+OVERLAY_ID+" select{width:100%;font:inherit;border:1px solid #ded7ca;border-radius:12px;padding:11px;background:#fff;color:#171326}#"+OVERLAY_ID+" .nd-actions{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:16px}#"+OVERLAY_ID+" button{font:inherit}#"+OVERLAY_ID+" .nd-save{border:0;border-radius:16px;background:#c4a65d;color:#171326;font-weight:800;min-height:48px}#"+OVERLAY_ID+" .nd-cancel{border:1px solid #ded7ca;border-radius:16px;background:#fff;color:#5d5668;font-weight:800;min-height:48px}#"+OVERLAY_ID+" .nd-message{margin-top:10px;font-size:13px;min-height:20px;color:#6d6471}";document.head.appendChild(style)}function renderOverlay(data){styles();document.getElementById(OVERLAY_ID)?.remove();let overlay=document.createElement("div");overlay.id=OVERLAY_ID;let enabled=new Set(data.preferences.enabled_divinations||[data.preferences.primary_divination||"numerology"]);let primary=data.preferences.primary_divination||Array.from(enabled)[0]||"numerology";let locked=data.actualPlan==="free"&&!data.adminMode;overlay.innerHTML=`<section class="nd-panel" role="dialog" aria-modal="true"><div class="nd-head"><div><h2>占術設定・変更</h2><p>どの画面からでも占術設定を変更できる補助パネルです。</p></div><button class="nd-close" type="button" aria-label="閉じる">×</button></div><div class="nd-status">${data.adminMode?"ADMIN PREVIEWとして占術変更を許可しています。実契約は"+data.actualPlan+"のままです。":locked?"Freeでは初回に選んだメイン占術を固定します。Proで変更できます。":"現在のプランでは占術変更できます。"}</div><div class="nd-options">${OPTIONS.map(option=>`<label class="nd-option"><input type="checkbox" value="${option.id}" ${enabled.has(option.id)?"checked":""} ${locked?"disabled":""}><div><strong>${option.label}</strong><span>${option.desc}</span></div></label>`).join("")}</div><label class="nd-primary">メイン占術<select ${locked?"disabled":""}>${OPTIONS.map(option=>`<option value="${option.id}" ${option.id===primary?"selected":""}>${option.label}</option>`).join("")}</select></label><div class="nd-actions"><button class="nd-cancel" type="button">閉じる</button><button class="nd-save" type="button" ${locked?"disabled":""}>保存</button></div><div class="nd-message"></div></section>`;document.body.appendChild(overlay);let close=()=>overlay.remove();overlay.querySelector(".nd-close").addEventListener("click",close);overlay.querySelector(".nd-cancel").addEventListener("click",close);overlay.addEventListener("click",event=>{if(event.target===overlay)close()});let message=overlay.querySelector(".nd-message");overlay.querySelector(".nd-save").addEventListener("click",async()=>{try{let selected=Array.from(overlay.querySelectorAll("input[type=checkbox]:checked")).map(input=>input.value);let chosenPrimary=overlay.querySelector("select").value;if(!selected.includes(chosenPrimary))selected.unshift(chosenPrimary);selected=[...new Set(selected)];if(selected.length===0)selected=[chosenPrimary||"numerology"];message.textContent="保存しています…";let headers=await authHeaders();await fetchJson("/api/user-preferences",{method:"PUT",headers,body:JSON.stringify({workspaceId:"ws_personal",primary_divination:chosenPrimary,enabled_divinations:selected})});message.textContent="保存しました。画面を再読み込みすると反映されます。";window.setTimeout(()=>window.location.reload(),800)}catch(error){message.textContent=String(error&&error.message||"保存できませんでした")}})}async function openPanel(){try{renderOverlay(await state())}catch(error){styles();let overlay=document.createElement("div");overlay.id=OVERLAY_ID;overlay.innerHTML=`<section class="nd-panel"><div class="nd-head"><div><h2>占術設定・変更</h2><p>設定画面を開けませんでした。</p></div><button class="nd-close" type="button">×</button></div><div class="nd-status">${String(error&&error.message||error)}</div></section>`;document.body.appendChild(overlay);overlay.querySelector(".nd-close").addEventListener("click",()=>overlay.remove())}}document.addEventListener("click",event=>{let node=event.target&&event.target.closest?event.target.closest("button,a,[role=button],li,div,span"):event.target;if(!node||!isTarget(node))return;event.preventDefault();event.stopPropagation();openPanel()},true);window.NumeriaOpenDivinationSettingsFallback=openPanel;})();</script>`;

const injection = `<!-- ${marker} -->${script}`;
if (html.includes("</body>")) {
  html = html.replace("</body>", `${injection}</body>`);
} else {
  html += injection;
}
writeFileSync(htmlPath, html);
console.log("Divination settings menu fallback injected into production HTML.");
