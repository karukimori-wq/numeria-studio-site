# Legacy Static Backup

The original ChatGPT Sites public build was committed before the editable Vite migration.

The current app entrypoint is the editable Vite file at the repository root:

- `index.html`
- `src/main.jsx`
- `src/styles.css`

The original public assets remain in `assets/`, and the full pre-migration HTML is preserved in
Git history and the exported backup zip.
